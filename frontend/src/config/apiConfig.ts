import { API_BASE_URL, BASE_URL } from '@/config/siteConfig';

export { API_BASE_URL, BASE_URL };

export const AUTH_URL = `${API_BASE_URL}/auth/login/`;
export const ADMIN_API_URL = `${API_BASE_URL}/admin`;
